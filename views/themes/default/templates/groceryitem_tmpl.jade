script#groceryitem_box(type='text/html')
  .groceryitem-box(data-user='{{ nickname }}', data-provider='{{ provider }}', data-type='{{ type }}')
    div(class="avatar avatar-{{ provider }}")
      img(src='http://avatars.io/{{ provider }}/{{ nickname }}')
      a(href="https://{{ provider }}.com/{{ nickname }}")
    h5
      | {{ nickname }}
    {{> groceryitem_box_text }}

script#groceryitem_box_text(type='text/html')
  p
    span.date {{ time.hours }}:{{ time.minutes }} {{ time.meridiem }}
    | {{ msg }}

script#groceryitem_notice(type='text/html')
  .notice(data-user="{{ user }}")
    span.date {{ time.hours }}:{{ time.minutes }} {{ time.meridiem }}
    |  {{ noticeMsg }}

script#people_box(type='text/html')
  a(class='{{ status }}', href='#', data-username='{{ nickname }}', data-provider='{{ provider }}')
    b
    | {{ nickname }}

script#title_template(type='text/html')
  | {{#count}} ({{ count }}) {{/count}} {{ grocerylistName }} - Balloons.IO
